LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_MODULE    := lory
LOCAL_SRC_FILES := $(wildcard $(LOCAL_PATH)/lory/*.c)
LOCAL_CFLAGS    := -Wall -std=c99
LOCAL_LDLIBS    := -llog -lm -ljnigraphics

include $(BUILD_SHARED_LIBRARY)
